services:
  oof-generator:
    image: ghcr.io/dingp/swim-meet-order-of-finish-generator:latest
    ports:
      - "8000:8000"
    environment:
      PORT: "8000"
      MAX_UPLOAD_MB: "20"
    restart: unless-stopped
